<div class="w-[480px] mx-auto">
  <div class="flex justify-between mb-6 items-center">
    <div class="text-2xl font-medium text-blue-500">About</div>
    <div><%= link_to "Edit", edit_product_path(@product), class: "max-w-max underline" %></div>
  </div>
  <div class="text-base mb-6">Name: <%= product.name %></div>
  <div class="text-base mb-6">Price: <%= product.price %></div>
  <div class="text-base mb-6">Description: <%= product.description %></div>
</div