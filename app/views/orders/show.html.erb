<div>
    <h1 class="text-2xl text-blue-500 mb-2">Show Order</h1>
    <div class="flex gap-x-4 mb-2 p-2 border border-gray-500 rounded-lg">
        <h1>Product name : <%= @order.product.name %></h1>
        <h1>Product description : <%= @order.product.description %></h1>
        <h1>Product price : <%= @order.product.price %></h1>
        <h1>Amount : <%= @order.amount %></h1>
        <h1>Total price : <%= @order.total_price %></h1>
    </div>
    <div class="border border-gray-500 rounded-lg p-2 mt-4 max-w-max">
        <%= link_to "Back to products list", products_path %>
    </div>
    
</div>